# 시나리오 2 확장 필요 목록 ver3 - 필수 최소 확장 및 AI 검증 가능성 검토

## 1. 정리 기준

```text
1. 현재 시나리오 1 기반 v2 인프라를 최대한 재사용한다.
2. 기존 필수 최소 확장 목록을 기준으로 유지한다.
3. 추합전 문서의 GWISIN 내부 파일 확산 개념은 필요한 부분만 흡수한다.
4. OTA 악성 펌웨어 배포 검증과 직접 관련이 약한 항목은 제거한다.
5. AI 1이 불변식 위반을 명확히 판단하고, AI 2가 GWISIN → OTA 배포 체인을 도출할 수 있는 필드만 남긴다.
6. 신규 자산을 과도하게 늘리지 않고, 기존 event_type 보강과 최소 신규 event_type 중심으로 구성한다.
```

---

## 2. 시나리오 2에 필요한 기존 불변식 목록

1. **INV-STD-02** 관리자·개발자·운영자·OTA 배포 계정은 직무 목적을 초과한 권한을 가지면 안 된다.
2. **INV-STD-03** 서비스 credential, OTA 배포 토큰, 펌웨어 서명키는 승인된 보관·관리 체계 외부에 저장되면 안 된다.
3. **INV-STD-04** 퇴사자·권한 변경자의 OTA 권한, 배포 credential, 서명 관련 접근 권한은 즉시 폐기 또는 회전되어야 한다.
4. **INV-STD-08** 내부 API와 보호 대상 업무 API는 승인된 경로 외부에서 직접 호출 가능하면 안 된다.
5. **INV-STD-10** OTA 관리자 API, 펌웨어 등록, 서명 요청, 배포 정책 변경, 기기 다운로드 로그는 추적 가능하게 기록되어야 한다.
6. **INV-STD-11** OTA 사고 관련 로그는 보존되어야 하며 무단 수정·삭제되면 안 된다.
7. **INV-STD-14** OTA 서버, 배포 서버, 서명 시스템, 로그 시스템 접근은 승인된 관리자와 허용된 경로로 제한되어야 한다.
8. **INV-ARG-08** OTA 업데이트 대상 정보는 권한 없는 사용자 또는 일반 고객 토큰에 노출되면 안 된다.

---

## 3. 시나리오 2 추가 불변식 목록

1. **INV-STD-CAND-WEB-01** 인터넷 노출 웹서비스의 웹 프로세스는 승인되지 않은 OS 명령 실행을 수행할 수 없어야 한다.
2. **INV-STD-CAND-WEB-02** 인터넷 노출 웹서비스는 승인되지 않은 실행 파일 또는 스크립트 업로드를 허용하면 안 된다.
3. **INV-STD-CAND-WEB-03** 인터넷 노출 웹서비스의 웹 프로세스는 승인된 경로 외부에 파일을 생성하거나 수정할 수 없어야 한다.
4. **INV-ARG-S2-01** `argos-dmz`는 승인된 경로 없이 `argos-ops`, `argos-deploy`, `argos-mgmt`, `argos-data`로 직접 접근할 수 없어야 한다.
5. **INV-ARG-S2-02** `argos-signing`은 일반 서버에서 직접 접근할 수 없어야 하며, 펌웨어 서명은 승인된 signing workflow를 통해서만 요청되어야 한다.
6. **INV-ARG-S2-03** 고객지원 포털 또는 `argos-ops` 계정은 OTA 캠페인 생성, 펌웨어 교체, 배포 대상 변경을 수행할 수 없어야 한다.
7. **INV-ARG-S2-04** 내부 HTTP/IIS/파일 공유 배포 경로는 승인된 파일 해시와 승인된 변경 이력이 있는 파일만 제공해야 하며, 내부 서버가 동일 `file_hash`를 다수 대상에 제공하는 배포 지점처럼 동작하면 탐지되어야 한다.
8. **INV-ARG-S2-05** OTA 서버는 승인된 CI/CD 산출물, 승인된 firmware hash, 유효한 펌웨어 서명값을 모두 만족하는 파일만 배포해야 한다.
9. **INV-ARG-S2-06** 펌웨어 서명키는 `argos-signing` 또는 승인된 KMS/HSM에서만 사용되어야 하며 `argos-ops`, `argos-dmz`, Git, CI 로그, 개발자 PC에 노출되면 안 된다.
10. **INV-ARG-S2-07** OTA 배포 대상 `tenant_id`, `device group`, `device model`은 해당 펌웨어의 허용 범위를 벗어나면 안 된다.
11. **INV-ARG-S2-08** 고위험 OTA 정책 변경은 승인 이력 없이 적용되면 안 된다.
12. **INV-ARG-S2-09** 기기는 서명 검증 실패, 승인되지 않은 OTA 캠페인, rollback 정책 위반 펌웨어를 다운로드하거나 적용하면 안 된다.
13. **INV-ARG-S2-10** `argos-security`는 웹 침해, 내부 정찰, credential 오용, 비인가 파일 생성, 내부 배포 지점화, 펌웨어 등록, 서명, 정책 변경, 기기 다운로드 이벤트를 `trace_id`, `deployment_id`, `file_hash`로 연결할 수 있어야 한다.
14. **INV-ARG-S2-11** OTA 관리자, 배포 서비스 계정, 관리 계정은 `argos-dmz`, 고객지원 포털, 웹 프로세스 출처에서 인증 성공하거나 고위험 작업을 수행할 수 없어야 한다.
15. **INV-ARG-S2-12** 내부 HTTP/IIS/파일 공유 배포 경로 또는 `argos-ops` 출처 파일은 정식 firmware catalog 또는 CI/CD artifact registry 등록 없이 OTA 배포 경로에 유입되면 안 된다.
16. **INV-ARG-S2-13** 고위험 OTA 정책 변경은 작업 실행자와 승인자가 동일하면 안 된다.

---

## 4. 필수 최소 확장 자산 목록

| 번호 | 필수 확장 자산 | 예시 | 역할 및 필요 이유 |
|---:|---|---|---|
| 1 | OTA Server / OTA Admin API | `/admin/ota/firmwares`, `/admin/ota/campaigns` | 펌웨어 등록, 캠페인 생성, 정책 변경의 실제 판단 지점 |
| 2 | Firmware Catalog | `firmware_catalog` table | 승인된 펌웨어 hash, version, signature 상태 판단 |
| 3 | OTA Deployment / Policy Store | `ota_deployments`, `ota_policy_events` | rollout, force update, rollback disable, 승인 이력 검증 |
| 4 | Firmware Signing Evidence | `argos-signing/signing_events.jsonl` | 서명 요청, workflow 승인 여부, key 사용 위치, 직접 접근 차단 여부 검증 |
| 5 | Firmware Signing Key Metadata | `key_id=fw-sign-prod-01` | 키 원문 없이 key 위치·노출·사용 상태 판단 |
| 6 | Device Inventory Extension | `devices.device_group`, `current_firmware_version`, `min_allowed_version` | tenant/model/group/rollback 대상 검증 |
| 7 | Firmware Metadata / Allowed Scope | `allowed_models=["homecam-v2"]` | 펌웨어별 허용 tenant, model, group 범위 검증 |
| 8 | Mock Device Update Source | `device_update_events.jsonl` | 실제 기기 감염 없이 다운로드·설치·rollback 검증 결과 확인 |
| 9 | 기존 `key_management_state` 보강 | `key_location_type`, `key_exposed`, `observed_location` | 별도 secret scan 없이 서명키 노출 판단 |
| 10 | Normalizer Mapping Extension | `source.event_type=firmware_signing_event` | 신규 OTA Evidence를 AI 입력 schema로 변환 |
| 11 | 기존 `gateway_access_event` 보강 | `request_action`, `file_type`, `upload_result`, `webshell_indicator` | 웹 최초 침해와 위험 업로드 근거 제공 |
| 12 | 기존 `log_retention_state` 재사용·보강 | `central_collected`, `tamper_protected`, `log_deleted_after_incident` | OTA 사고 로그 보존·변조·삭제 여부 판단 |

---

## 5. 선택 확장 1순위 자산 목록

추합전 문서에서 흡수할 가치가 큰 항목만 남긴다.

| 번호 | 선택 확장 자산 | 예시 | 선택 1순위로 남기는 이유 |
|---:|---|---|---|
| 1 | 비인가 파일 마커 / 파일 무결성 이벤트 | `file_integrity_event`, `file_hash=sha256:bad...` | GWISIN식 내부 파일 생성·확산 흐름을 안전하게 표현 |
| 2 | 내부 서버 간 통신 허용 목록 | `network_policy_state`, `business_purpose`, `approved` | `argos-dmz` 또는 `argos-ops`에서 내부 중요망 접근이 승인 경로인지 판단 |
| 3 | 내부 배포 지점화 탐지 룰 | `distribution_point_suspected=true` | 내부 서버가 임시 파일 배포소처럼 동작하는지 탐지 |
| 4 | `trace_id` / `file_hash` 상관관계 키 | `trace_id=trc-s2-001`, `file_hash=sha256:bad...` | 웹 침해 → 파일 생성 → 내부 배포 → OTA 유입을 하나의 흐름으로 연결 |
| 5 | 웹 프로세스 실행 탐지 | `process_execution_event` 또는 `wazuh_alert_event` | 웹 프로세스 명령 실행을 직접 판단하고 싶을 때 사용 |

---

## 6. 제거한 과확장 자산 목록

| 제거 항목 | 제거 이유 |
|---|---|
| 영상/센서 처리 서버 및 저장소 확장 | OTA 악성 펌웨어 배포 본체와 직접성이 약함 |
| 서비스 장애 전파 상태 | GWISIN 업무 영향 설명에는 좋지만 현재 OTA 배포 검증에는 과함 |
| 백업 및 복구 상태 | 랜섬웨어 관점에서는 중요하지만 이번 확정 시나리오는 OTA 배포 무결성 검증 중심 |
| 실제 CI/CD 파이프라인 상세 로그 전체 | 5일 구현 범위에서 과함. `artifact_registered`, `approved_artifact`로 축약 가능 |
| 실제 공급망 소프트웨어 이벤트 | 이번 시나리오 2 본체 범위를 넘어감 |
| 실제 기기 상세 telemetry | mock `device_update_event`로 충분 |
| 독립 `service_health_state` | AI 1 불변식 판단에 직접 필요하지 않음 |
| 독립 `backup_restore_state` | `log_retention_state`와 목적이 다르고 이번 본체에서 제외 |

---

## 7. 필수 최소 수집 로그 데이터와 상태 목록

| 번호 | event_type 또는 기존 event 보강 | 예시 필드 | 예시값 | 필요 이유 |
|---:|---|---|---|---|
| 1 | `gateway_access_event` 보강 | `source_ip`, `uri`, `request_action`, `file_type`, `upload_result`, `webshell_indicator` | `203.0.113.10`, `/support/upload`, `upload`, `script`, `blocked`, `true` | 웹 최초 침해, 위험 업로드, 웹셸 의심 요청 판단 |
| 2 | `wazuh_alert_event` 또는 `process_execution_event` | `process_role`, `command_category`, `execution_result`, `linked_request_id` | `web_process`, `command_execution`, `blocked`, `req-s2-web-001` | 웹 프로세스의 승인되지 않은 OS 명령 실행 판단 |
| 3 | `file_integrity_event` | `file_hash`, `origin_service`, `file_event`, `file_path_group`, `path_approved`, `approved_artifact` | `sha256:bad...`, `support_portal`, `create`, `distribution`, `false`, `false` | 경로 외 파일 생성, 비인가 파일 마커, 내부 파일 확산 판단 |
| 4 | `network_policy_state` 또는 `internal_access_event` | `source_asset`, `dest_asset`, `business_purpose`, `approved`, `access_path`, `access_decision` | `argos-dmz`, `argos-deploy`, `none`, `false`, `direct`, `denied` | DMZ/OPS에서 내부망 직접 접근 및 승인 경로 여부 판단 |
| 5 | `firmware_register_event` | `actor_id`, `actor_role`, `source_asset`, `origin_service`, `file_origin`, `firmware_id`, `firmware_hash`, `catalog_registered`, `artifact_registered`, `approved_artifact`, `authorization_decision` | `support01`, `support_admin`, `argos-ops`, `support_portal`, `internal_http`, `fw-homecam-2.4.1`, `sha256:bad...`, `false`, `false`, `false`, `deny` | 내부 출처 파일이 OTA 등록으로 유입되는지 판단 |
| 6 | `firmware_signing_event` | `workflow_id`, `workflow_approved`, `key_id`, `key_location_type`, `access_path`, `access_decision`, `caller_allowed`, `signing_result` | `sgn-wf-001`, `false`, `fw-sign-prod-01`, `argos-signing`, `direct`, `denied`, `false`, `denied` | signing workflow와 signing 직접 접근 차단 판단 |
| 7 | `firmware_catalog_state` | `firmware_id`, `firmware_hash`, `signature_status`, `approval_status`, `artifact_id`, `allowed_models`, `allowed_tenant_ids`, `allowed_device_groups` | `fw-homecam-2.4.1`, `sha256:8f2a...`, `valid`, `approved`, `art-jenkins-2041`, `homecam-v2`, `tenant-a`, `homecam-beta` | 승인 펌웨어와 허용 대상 기준 제공 |
| 8 | `ota_policy_event` | `deployment_id`, `firmware_id`, `tenant_id`, `target_group`, `device_model`, `policy_name`, `rollout_percent`, `force_update`, `rollback_disabled`, `approval_id`, `approval_status`, `approval_time`, `applied_time`, `actor_id`, `approver_id` | `dep-001`, `fw-homecam-2.4.1`, `tenant-a`, `homecam-beta`, `homecam-v2`, `rollout_100`, `100`, `true`, `true`, `apv-014`, `approved`, `2026-05-07T15:30:00+09:00`, `2026-05-07T15:40:00+09:00`, `ota-admin01`, `sec-approver01` | 대상 범위, 고위험 정책 승인, 승인 선후 관계, 승인자 분리 검증 |
| 9 | `device_update_event` | `device_id`, `deployment_id`, `firmware_id`, `firmware_hash`, `signature_status`, `campaign_approval_status`, `current_version`, `target_version`, `min_allowed_version`, `download_result`, `install_result`, `validation_failure_reason` | `dev-cam-001`, `dep-001`, `fw-homecam-2.4.1`, `sha256:bad...`, `invalid`, `missing`, `2.4.1`, `2.3.0`, `2.4.0`, `blocked`, `blocked`, `rollback_violation` | 기기 서명 검증, 승인 캠페인, rollback 정책 위반 판단 |
| 10 | `key_management_state` 보강 | `key_id`, `key_location_type`, `key_exposed`, `observed_location` | `fw-sign-prod-01`, `argos-signing`, `false`, `argos-signing` | 서명키가 승인 위치 외부에 노출됐는지 판단 |
| 11 | `api_authorization_event` 보강 | `endpoint`, `actor_role`, `request_action`, `authorization_decision`, `can_modify_ota_campaign`, `can_publish_artifact` | `/admin/ota/campaigns`, `support_admin`, `create_ota_campaign`, `deny`, `false`, `false` | 고객지원/ops 계정의 OTA 조작 및 artifact 등록 권한 판단 |
| 12 | `identity_auth_event` 보강 | `actor_role`, `source_context`, `auth_result`, `high_risk_action_result` | `ota_admin`, `web_process`, `blocked`, `denied` | 중요 계정이 DMZ/웹 출처에서 사용됐는지 판단 |
| 13 | `file_distribution_state` | `file_hash`, `same_hash_host_count`, `distribution_point_suspected`, `approved_artifact`, `change_ticket_id`, `approval_status` | `sha256:bad...`, `5`, `true`, `false`, `null`, `missing` | 내부 배포 지점화 및 승인 변경 이력 없는 파일 제공 판단 |
| 14 | `aggregation_state` 보강 | `file_hash`, `same_hash_host_count`, `distinct_destination_count`, `source_host`, `alert_created` | `sha256:bad...`, `5`, `4`, `argos-ops`, `true` | 동일 파일 hash가 여러 서버로 확산됐는지 파생 집계 |
| 15 | `log_trace_state` 보강 | `trace_id`, `deployment_id`, `file_hash`, `missing_stage`, `log_trace_gap` | `trc-s2-001`, `dep-001`, `sha256:bad...`, `signing_log`, `true` | 웹 침해부터 OTA 배포까지 체인 연결성 판단 |
| 16 | `log_retention_state` | `central_collected`, `tamper_protected`, `log_deleted_after_incident` | `true`, `true`, `false` | OTA 사고 관련 로그 보존·변조·삭제 여부 판단 |

---

## 8. 선택 수집 로그 데이터와 상태 목록

| 번호 | event_type 또는 기존 event 보강 | 예시 필드 | 예시값 | 선택 이유 |
|---:|---|---|---|---|
| 1 | `file_integrity_event` | `file_hash`, `origin_service`, `file_path_group`, `path_approved`, `approved_artifact` | `sha256:bad...`, `support_portal`, `distribution`, `false`, `false` | 고객지원/ops 출처 비인가 파일이 생성됐는지 검증 |
| 2 | `network_policy_state` 또는 `internal_access_event` | `source_asset`, `dest_asset`, `business_purpose`, `approved`, `access_decision` | `argos-dmz`, `argos-deploy`, `none`, `false`, `denied` | 내부 통신 허용 목록 기반 접근 검증 |
| 3 | `file_distribution_state` | `file_hash`, `same_hash_host_count`, `distribution_point_suspected`, `approved_artifact` | `sha256:bad...`, `5`, `true`, `false` | 내부 서버 배포 지점화 탐지 |
| 4 | `aggregation_state` 보강 | `same_hash_host_count`, `distinct_destination_count`, `source_host`, `alert_created` | `5`, `4`, `argos-ops`, `true` | 여러 서버로 동일 파일이 확산됐는지 파생 집계 |
| 5 | `firmware_register_event` 보강 | `file_origin`, `origin_service`, `approved_artifact` | `internal_http`, `argos-ops`, `false` | 내부 출처 파일이 OTA 등록으로 유입됐는지 검증 |
| 6 | `wazuh_alert_event` | `rule_id`, `alert_type`, `linked_request_id`, `linked_file_hash` | `argos-web-001`, `web_process_command_execution`, `req-s2-web-001`, `sha256:bad...` | 별도 `process_execution_event` 없이 웹 명령 실행 탐지를 표현할 때 사용 |

---

## 9. 최종 필드 보강 목록

| 필드 | 흡수 위치 | 필요 이유 |
|---|---|---|
| `file_hash` | `firmware_register_event`, `firmware_catalog_state`, `device_update_event`, `log_trace_state` | 파일 확산과 OTA 펌웨어 hash를 연결 |
| `origin_service` | `file_integrity_event`, `firmware_register_event` | 고객지원 포털/`argos-ops` 출처 여부 판단 |
| `approved_artifact` | `firmware_register_event`, `file_distribution_state` | 정식 CI/CD 산출물 여부 판단 |
| `same_hash_host_count` | `aggregation_state`, `file_distribution_state` | 동일 파일이 여러 서버에 퍼졌는지 판단 |
| `distribution_point_suspected` | `file_distribution_state`, `aggregation_state` | 내부 서버가 배포소처럼 동작했는지 판단 |
| `business_purpose` | `network_policy_state`, `internal_access_event` | 내부 연결이 업무 목적 기반인지 판단 |
| `can_modify_ota_campaign` | `api_authorization_event`, `privileged_access_state` | 고객지원/ops 계정의 OTA 조작 권한 여부 판단 |
| `can_publish_artifact` | `privileged_access_state`, `api_authorization_event` | 고객지원/ops 계정의 artifact 등록 권한 여부 판단 |
| `request_action` | `gateway_access_event`, `api_authorization_event` | 업로드, 캠페인 생성, 펌웨어 등록 등 행위 구분 |
| `file_type` | `gateway_access_event`, `file_integrity_event` | 스크립트·실행 파일·펌웨어 stub 구분 |
| `upload_result` | `gateway_access_event` | 위험 파일 업로드 차단 여부 판단 |
| `webshell_indicator` | `gateway_access_event`, `wazuh_alert_event` | 웹셸 의심 요청 여부 판단 |
| `access_path` | `internal_access_event`, `firmware_signing_event` | 승인 경로인지 직접 접근인지 판단 |
| `access_decision` | `internal_access_event`, `firmware_signing_event` | 접근 차단 여부 판단 |
| `approval_time` | `ota_policy_event` | 승인 시각 확인 |
| `applied_time` | `ota_policy_event` | 승인 전 적용 여부 판단 |
| `current_version` | `device_update_event` | rollback 판단 기준 |
| `target_version` | `device_update_event` | 적용 대상 버전 |
| `min_allowed_version` | `device_update_event`, `device_inventory_state` | rollback 정책 위반 판단 |
| `central_collected` | `log_retention_state` | 중앙 수집 여부 판단 |
| `tamper_protected` | `log_retention_state` | 로그 변조 방지 여부 판단 |
| `log_deleted_after_incident` | `log_retention_state` | 사고 이후 로그 삭제 여부 판단 |

---

## 10. 확장 자산 예시

```json
{
  "asset_id": "firmware_catalog",
  "location": "argos-data.postgresql",
  "example_record": {
    "firmware_id": "fw-homecam-2.4.1",
    "firmware_hash": "sha256:8f2a9c...",
    "firmware_version": "2.4.1",
    "signature_status": "valid",
    "approval_status": "approved",
    "allowed_tenant_ids": ["tenant-a"],
    "allowed_models": ["homecam-v2"],
    "allowed_device_groups": ["homecam-beta"],
    "artifact_id": "art-jenkins-2041"
  }
}
```

```json
{
  "asset_id": "ota_policy_store",
  "location": "argos-deploy or argos-data",
  "example_record": {
    "deployment_id": "dep-ota-20260507-001",
    "firmware_id": "fw-homecam-2.4.1",
    "target_group": "homecam-beta",
    "tenant_id": "tenant-a",
    "device_model": "homecam-v2",
    "rollout_percent": 100,
    "force_update": true,
    "rollback_disabled": true,
    "approval_id": "apv-20260507-014",
    "approval_status": "approved",
    "approval_time": "2026-05-07T15:30:00+09:00",
    "applied_time": "2026-05-07T15:40:00+09:00",
    "actor_id": "ota-admin01",
    "approver_id": "sec-approver01"
  }
}
```

```json
{
  "asset_id": "file_distribution_state",
  "location": "argos-security.derived",
  "example_record": {
    "file_hash": "sha256:bad...",
    "source_host": "argos-ops",
    "same_hash_host_count": 5,
    "distinct_destination_count": 4,
    "approved_artifact": false,
    "distribution_point_suspected": true,
    "change_ticket_id": null,
    "approval_status": "missing",
    "alert_created": true
  }
}
```

---

## 11. 추가 수집 이벤트 예시

```json
{
  "schema_version": "argos-siem-event-v1",
  "event_type": "gateway_access_event",
  "trace_id": "trc-s2-001",
  "request_id": "req-s2-web-001",
  "producer": {
    "vm": "argos-dmz",
    "component_id": "dmz-nginx"
  },
  "observed": {
    "source_ip": "203.0.113.10",
    "uri": "/support/upload",
    "request_action": "upload",
    "file_type": "script",
    "upload_result": "blocked",
    "webshell_indicator": true
  }
}
```

```json
{
  "schema_version": "argos-siem-event-v1",
  "event_type": "firmware_register_event",
  "trace_id": "trc-s2-001",
  "request_id": "req-s2-031",
  "producer": {
    "vm": "argos-deploy",
    "component_id": "ota-admin-api"
  },
  "observed": {
    "actor_id": "support01",
    "actor_role": "support_admin",
    "source_asset": "argos-ops",
    "origin_service": "support_portal",
    "file_origin": "internal_http",
    "firmware_id": "fw-homecam-2.4.1",
    "firmware_hash": "sha256:bad...",
    "catalog_registered": false,
    "artifact_registered": false,
    "approved_artifact": false,
    "authorization_decision": "deny"
  }
}
```

```json
{
  "schema_version": "argos-siem-event-v1",
  "event_type": "firmware_signing_event",
  "trace_id": "trc-s2-001",
  "request_id": "req-s2-041",
  "producer": {
    "vm": "argos-signing",
    "component_id": "firmware-signing-service"
  },
  "observed": {
    "signing_request_id": "sgn-req-001",
    "source_asset": "argos-deploy",
    "workflow_id": "sgn-wf-20260507-001",
    "workflow_approved": false,
    "firmware_id": "fw-homecam-2.4.1",
    "firmware_hash": "sha256:bad...",
    "key_id": "fw-sign-prod-01",
    "key_location_type": "argos-signing",
    "access_path": "direct",
    "access_decision": "denied",
    "caller_allowed": false,
    "signing_result": "denied"
  }
}
```

```json
{
  "schema_version": "argos-siem-event-v1",
  "event_type": "ota_policy_event",
  "trace_id": "trc-s2-001",
  "request_id": "req-s2-policy-001",
  "producer": {
    "vm": "argos-deploy",
    "component_id": "ota-policy-service"
  },
  "observed": {
    "deployment_id": "dep-001",
    "firmware_id": "fw-homecam-2.4.1",
    "tenant_id": "tenant-a",
    "target_group": "homecam-beta",
    "device_model": "homecam-v2",
    "policy_name": "rollout_100",
    "rollout_percent": 100,
    "force_update": true,
    "rollback_disabled": true,
    "approval_id": "apv-014",
    "approval_status": "approved",
    "approval_time": "2026-05-07T15:30:00+09:00",
    "applied_time": "2026-05-07T15:40:00+09:00",
    "actor_id": "ota-admin01",
    "approver_id": "sec-approver01"
  }
}
```

```json
{
  "schema_version": "argos-siem-event-v1",
  "event_type": "device_update_event",
  "trace_id": "trc-s2-001",
  "producer": {
    "vm": "argos-iot",
    "component_id": "mock-device-updater"
  },
  "observed": {
    "device_id": "dev-cam-001",
    "deployment_id": "dep-001",
    "firmware_id": "fw-homecam-2.4.1",
    "firmware_hash": "sha256:bad...",
    "signature_status": "invalid",
    "campaign_approval_status": "missing",
    "current_version": "2.4.1",
    "target_version": "2.3.0",
    "min_allowed_version": "2.4.0",
    "download_result": "blocked",
    "install_result": "blocked",
    "validation_failure_reason": "rollback_violation"
  }
}
```

```json
{
  "schema_version": "argos-siem-event-v1",
  "event_type": "log_retention_state",
  "trace_id": "trc-s2-001",
  "producer": {
    "vm": "argos-security",
    "component_id": "evidence-retention-checker"
  },
  "observed": {
    "central_collected": true,
    "tamper_protected": true,
    "log_deleted_after_incident": false
  }
}
```

---

## 12. AI 1 불변식 검증 가능성 검토

| 검증 영역 | 필요 Evidence | 판단 가능 여부 |
|---|---|---|
| 웹 위험 업로드 차단 | `gateway_access_event.request_action`, `file_type`, `upload_result`, `webshell_indicator` | 가능 |
| 웹 프로세스 명령 실행 차단 | `process_execution_event.command_category` 또는 `wazuh_alert_event.alert_type` | 가능 |
| 경로 외 파일 생성·수정 차단 | `file_integrity_event.file_path_group`, `path_approved`, `file_event` | 가능 |
| DMZ/OPS 내부 직접 접근 차단 | `internal_access_event` 또는 `network_policy_state`의 `access_path`, `access_decision`, `approved` | 가능 |
| signing 직접 접근 차단 | `firmware_signing_event.access_path`, `access_decision`, `caller_allowed` | 가능 |
| OTA 조작 권한 초과 차단 | `api_authorization_event.can_modify_ota_campaign`, `authorization_decision` | 가능 |
| 내부 파일의 OTA 경로 유입 차단 | `firmware_register_event.file_origin`, `origin_service`, `approved_artifact`, `artifact_registered` | 가능 |
| 승인 artifact/hash/signature 검증 | `artifact_registered`, `catalog_registered`, `firmware_catalog_state.signature_status` | 가능 |
| 서명키 승인 위치·노출 검증 | `key_management_state.key_location_type`, `key_exposed`, `observed_location` | 가능 |
| OTA 대상 범위 검증 | `ota_policy_event.tenant_id`, `target_group`, `device_model` + `firmware_catalog_state.allowed_*` | 가능 |
| 고위험 OTA 정책 승인 검증 | `approval_id`, `approval_status`, `approval_time`, `applied_time` | 가능 |
| 실행자·승인자 분리 검증 | `ota_policy_event.actor_id`, `approver_id` | 가능 |
| 기기 서명·캠페인·rollback 검증 | `device_update_event.signature_status`, `campaign_approval_status`, `current_version`, `target_version`, `min_allowed_version` | 가능 |
| trace 연결성 검증 | `log_trace_state.trace_id`, `deployment_id`, `file_hash`, `missing_stage`, `log_trace_gap` | 가능 |
| 로그 보존·변조·삭제 검증 | `log_retention_state.central_collected`, `tamper_protected`, `log_deleted_after_incident` | 가능 |

**AI 1 판단 결론**

```text
ver3에 정의된 자산과 필드가 실제 Evidence로 수집된다면,
AI 1은 시나리오 2의 위반 불변식 결과를 도출할 수 있다.
```

---

## 13. AI 2 MITRE ATT&CK 절차 기반 시나리오 도출 가능성 검토

AI 2가 사용할 수 있는 주요 체인 키는 다음과 같다.

```text
trace_id
request_id
deployment_id
firmware_id
firmware_hash
file_hash
actor_id
source_asset
origin_service
```

도출 가능한 Evidence 체인은 다음과 같다.

```text
gateway_access_event
→ process_execution_event 또는 wazuh_alert_event
→ file_integrity_event
→ internal_access_event 또는 network_policy_state
→ api_authorization_event / identity_auth_event
→ file_distribution_state / aggregation_state
→ firmware_register_event
→ firmware_signing_event
→ firmware_catalog_state
→ ota_policy_event
→ device_update_event
→ log_trace_state / log_retention_state
```

| MITRE ATT&CK 전술 단계 | 연결 Evidence | 도출 가능 여부 |
|---|---|---|
| Reconnaissance | OTA 대상 정보, firmware catalog, deployment policy | 가능 |
| Resource Development | 미승인 `firmware_hash`, `approved_artifact=false`, 더미 firmware artifact | 가능 |
| Initial Access | `gateway_access_event`, `process_execution_event`, `file_integrity_event` | 가능 |
| Defense Evasion | `approval_status`, `rollback_disabled`, `log_trace_gap`, 정상 운영 API 위장 | 가능 |
| Credential Access | `identity_auth_event`, `key_management_state`, `key_exposed` | 가능 |
| Discovery | `internal_access_event`, `network_policy_state`, 내부 자산 접근 시도 | 가능 |
| Privilege Escalation | `api_authorization_event`, `can_modify_ota_campaign`, `can_publish_artifact` | 가능 |
| Collection | `firmware_catalog_state`, `ota_policy_event`, OTA 대상 필드 | 가능 |
| Command and Control | `file_distribution_state`, `firmware_register_event`, `firmware_signing_event` | 가능 |
| Exfiltration 해석 | 내부 파일의 OTA 신뢰 경계 유입, `approved_artifact=false` | 가능 |
| Impact | `ota_policy_event`, `device_update_event` | 가능 |
| Traceability | `log_trace_state`, `log_retention_state` | 가능 |

**AI 2 판단 결론**

```text
ver3 필드 세트는 AI 2가 MITRE ATT&CK 절차 기반 침투 시나리오 2를 도출하기에 충분하다.
특히 file_hash와 deployment_id가 함께 존재하므로,
GWISIN식 내부 파일 확산이 OTA 악성 펌웨어 배포 체계로 이어지는 흐름을 설명할 수 있다.
```

---

## 14. 최종 추합 방향

```text
불변식:
기존 필수 최소 확장 파일 기준을 유지한다.
추합전 불변식은 INV-ARG-S2-04, INV-ARG-S2-10, INV-ARG-S2-12의 근거와 설명에 흡수한다.
웹 최초 침해, signing 직접 접근, rollback, 로그 보존 검증을 위해 최소 보강 필드를 추가한다.

자산:
기존 필수 최소 자산 기준을 유지한다.
추합전 자산 중 비인가 파일 마커, 내부 통신 허용 목록, 내부 배포 지점화 탐지 룰, file_hash 상관관계 키만 선택 확장 1순위로 추가한다.
추가 자산을 더 늘리지 않고 gateway, log_retention, key_management 같은 기존 자산을 보강한다.

수집 데이터:
기존 필수 최소 event_type 기준을 유지한다.
추합전 수집 데이터 중 file_hash, origin_service, approved_artifact, same_hash_host_count, distribution_point_suspected, can_modify_ota_campaign, can_publish_artifact, business_purpose 필드를 보강한다.
AI 1 검증 공백을 막기 위해 request_action, file_type, upload_result, webshell_indicator, approval_time, applied_time, current_version, target_version, min_allowed_version, central_collected, tamper_protected, log_deleted_after_incident를 추가한다.

제거:
영상/센서 처리 확장, 백업/복구, 서비스 장애 전파, 실제 공급망 소프트웨어 이벤트, 실제 기기 telemetry, 전체 CI/CD 상세 로그는 이번 최종 시나리오 2 본체에서 제외한다.
```

---

## 15. 최종 판단

```text
1. 이 ver3 파일은 기존 ver2 파일의 구조를 유지하면서 최소 보강 필드를 반영한 결과다.
2. AI 1은 시나리오 2 불변식 위반 여부를 판단할 수 있다.
3. AI 2는 GWISIN형 내부 침투가 OTA 악성 펌웨어 배포로 이어지는 MITRE ATT&CK 절차 기반 시나리오를 도출할 수 있다.
4. 신규 자산을 과도하게 늘리지 않고 기존 시나리오 1 v2 인프라를 최대한 재사용한다.
5. 핵심 보강은 웹 최초 침해, OTA 대상 범위, rollback, 로그 보존, trace 연결성에 집중한다.
```

따라서 ver3에 포함된 자산과 필드를 확장해 수집한다면, AI 1의 위반 불변식 결과 도출과 AI 2의 MITRE ATT&CK 절차 기반 침투 시나리오 2 출력은 모두 가능하다.
